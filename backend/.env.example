# Copiar este archivo a ".env" (en la carpeta backend/) y completar con valores reales.
# NUNCA subir el archivo ".env" real al repositorio.

OPENAI_API_KEY=

# OBSOLETA: ya no se usa para resolver el Vector Store de una consulta por equipo (cada
# clase_detector usa su propio Vector Store, definido en
# backend/app/services/equipo_manual_map.py). Se deja aquí, vacía, solo como referencia; no
# hace falta completarla.
OPENAI_VECTOR_STORE_ID=

# Opcional: modelo de OpenAI a usar para las respuestas del asistente.
# OPENAI_MODEL=gpt-4o-mini
